# ISSUE-20260302 Planning Report / 规划报告

Date: 2026-03-02
Task: ISSUE-20260302
Stage: Plan

## EN (for agents)

### Objective
Create an actionable, phased plan to resolve:
1) stale/unreliable news evidence in multi-agent analysis;
2) contradictory day-to-day narrative flips.

### Actions Taken
1. Read and analyzed `20260302_ISSUE.txt` user issue list.
2. Converted the issue list into an implementation-ready plan with:
   - root-cause hypotheses,
   - ordered solution steps,
   - acceptance criteria,
   - test strategy,
   - phase-by-phase delivery sequence.
3. Synced automation tracking artifacts:
   - added `ISSUE-20260302` in `.automation/tasks.json` (`plan=done`),
   - updated `.automation/state.json` current task/report pointer.
4. Added one continuous actionable rule to `.github/copilot-instructions.md`:
   - enforce recency + source/timestamp fields and fallback to neutral when missing.
5. Incorporated user-confirmed constraint for consistency design:
   - only Commander Agent receives historical analysis context;
   - carry records from recent 3-7 days (default 5, configurable 3/5/7);
   - limit payload to compact fields and capped item count.

### Planned Execution Order
- Phase 1: freshness/reliability constraints (prompt + retrieval time filter + evidence fields).
- Phase 2: consistency controls (structured template + multi-timeframe consistency + revision rationale + Commander-only 3-7 day memory injection).
- Phase 3: integrated replay + UI verification and parameter tuning.

### Test Commands (Plan Stage)
- Command:
   - `$null = Get-Content ".automation\\tasks.json" -Raw | ConvertFrom-Json; $null = Get-Content ".automation\\state.json" -Raw | ConvertFrom-Json; Write-Output "JSON validation passed: tasks/state"`
- Result:
   - `JSON validation passed: tasks/state`
   - Status: PASS

### Issues / Risks
- Existing `state.json` has legacy branch/log metadata from older run; only `taskId/reportPath` updated for this task.
- Dev/test implementation details will be validated in subsequent dev report.

---

## 中文（给用户）

### 本次目标
把你提出的 2 个问题整理成“可执行、可验证、可追踪”的分阶段方案：
1）资讯时效与可靠性不足；
2）分析结论前后矛盾。

### 已执行动作
1. 读取并分析 `20260302_ISSUE.txt` 原始问题。
2. 将问题升级为可实施方案，包含：
   - 根因假设
   - 分步改造顺序
   - 验收标准
   - 测试策略
   - 分阶段实施节奏（Phase 1/2/3）
3. 同步自动化追踪文件：
   - 在 `.automation/tasks.json` 新增 `ISSUE-20260302`（已完成 plan 阶段）
   - 在 `.automation/state.json` 切换当前任务与报告指向
4. 按持续规则要求，新增一条规则到 `.github/copilot-instructions.md`：
   - 资讯结论必须有来源+发布时间；缺失时间戳则降级为中性/信息不足。
5. 根据你的确认，补充一致性设计约束：
   - 历史分析记录仅注入“指挥者 Agent”；
   - 注入窗口为近 3-7 天（默认 5 天，可配 3/5/7）；
   - 仅携带精简字段并限制条数，降低噪声与偏置。

### 后续实施顺序
- Phase 1：先做“资讯新鲜度与可信度”约束。
- Phase 2：再做“结论一致性与改判解释（含仅指挥者 Agent 的 3-7 天历史记忆注入）”。
- Phase 3：联调回放 + UI 验证 + 参数收敛。

### 测试命令（规划阶段）
- 命令：
   - `$null = Get-Content ".automation\\tasks.json" -Raw | ConvertFrom-Json; $null = Get-Content ".automation\\state.json" -Raw | ConvertFrom-Json; Write-Output "JSON validation passed: tasks/state"`
- 结果：
   - `JSON validation passed: tasks/state`
   - 状态：通过

### 风险说明
- `state.json` 中历史分支/日志字段来自旧运行记录，本次仅更新了任务与报告指向；不影响后续开发分阶段执行。
